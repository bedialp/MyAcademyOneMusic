@model List<Song>
<section class="featured-artist-area section-padding-100 bg-img bg-overlay bg-fixed" style="background-image: url(/one-music-gh-pages/img/bg-img/bg-4.jpg);">
	<div class="container">
		<div class="row align-items-end">
			<div class="col-12 col-md-5 col-lg-4">
				<div class="featured-artist-thumb">
					@foreach (var item in Model)
					{
						<img src="@item.Album.CoverImage" alt="">
					}
				</div>
			</div>

			<div class="col-12 col-md-7 col-lg-8">
				<div class="featured-artist-content">
					<!-- Section Heading -->
					<div class="section-heading white text-left mb-30">
						<p>Gelişigüzel</p>
						<h2>ŞANSLI PARCA</h2>
					</div>
					<p>Binlerce müzik arasından rastgele seçtiğimiz bu parcayı dinlemek ister misiniz?</p>
					@foreach (var item in Model)
					{
						<div class="song-play-area">
							<div class="song-name">
								<p>@item.Album.AlbumName</p>
							</div>
							<audio preload="auto" controls>
								<source src="/audio/@item.SongUrl">
							</audio>
						</div>
					}
				</div>
			</div>
		</div>
	</div>
</section>